seed: 42
device: 
  type: "cuda"
  ids : [3]

paths:
  run_root: "./runs/0313"

data:
  experiment: "stage1_utah_only"
  split_dir: "./data/0312/metadata/experiments/stage1_utah_only"
  num_workers: 4
  pin_memory: true
  normalize: "robust"

model:
  encoder:
    in_channels: 1
    num_layers: 4
    base_channels: 32
    latent_dim: 128
    norm: "bn"
    act: "relu"
    dropout: 0.0

  cae:
    mirror_decoder: true
    final_act: "identity"